extends layout

block content
    .container
        h1 Register
            form(method="POST", action="/user/login")
                .form-group
                    label Username
                    input.form-control(type="text" name="username" placeholder="输入用户名")
                .form-group
                    label Password
                    input.form-control(type="password" name="password" )
                input.btn.btn-primary(type="submit" value="提交")